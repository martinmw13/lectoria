---
name: git-usage
description: Git interactions — commits, PRs, descriptions, comments
---

# Interacting with Git

* When asked about git commits or pushing changes, see ./commit.md
* When asked about creating a PR or adding description/comments, see ./pr.md

Use `git` for pure Git operations and `gh` for GitHub interactions.
