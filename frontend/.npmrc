# openapi-typescript's peer range (typescript@^5.x) lags the repo's TypeScript 6.
# It only emits strings (no compiler API use), so the conflict is cosmetic.
# Relax peer resolution so `npm ci` (CI + the API-types drift gate) installs cleanly.
legacy-peer-deps=true
